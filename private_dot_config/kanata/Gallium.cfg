(defcfg
  ;; Place your OS specific configurations here, see: https://github.com/jtroo/kanata
  ;; Only grab the built-in MacBook keyboard; leave external keyboards (e.g. TOTEM) untouched.
  macos-dev-names-include (
    "Apple Internal Keyboard / Trackpad"
  )
)
;; The config below uses ANSI, if you are on ISO you will have to modify it.
(defsrc
  esc     f1     f2   f3   f4   f5   f6   f7   f8   f9   f10  f11  f12
  grv       1    2    3    4    5    6    7    8    9    0    -    =    bspc
  tab       q    w    e    r    t    y    u    i    o    p    [    ]    \
  caps      a    s    d    f    g    h    j    k    l    ;    '         ret
  lsft      z    x    c    v    b    n    m    ,    .    /    rsft
  lctl lmet lalt           spc                 ralt rmet rctl
)

(deflayer qwerty
  caps    brdown brup mctl f4   bldn blup prev pp   next mute vold volu
  @grl       1    2    3    4    5    6    7    8    9    0    -    =    bspc
  tab       q    w    e    r    t    y    u    i    o    p    [    ]    \
  @cec      a    s    d    f    g    h    j    k    l    ;    '         ret
  lsft      z    x    c    v    b    n    m    ,    .    /    rsft
  lctl lmet lalt           spc                 ralt rmet rctl
)

;; Simply uncomment the Gallium version you wish to use.

;; Gallium Colstag
(deflayer gallium
  caps    brdown brup mctl f4   bldn blup prev pp   next mute vold volu
  @grl       1    2    3    4    5    6    7    8    9    0    -    =    bspc
  tab       b    l    d    c    v    j    y    o    u    '    [    ]    \
  @cec      n    r    t    s    g    p    h    a    e    i    ;    ret
  lsft      x    q    m    w    z    k    f    ,    .    /    rsft
  lctl lmet lalt           spc                 ralt rmet rctl
)

(defalias
  ;; tap: backtick (grave), hold: toggle layer-switching layer while held
  grl (tap-hold 200 200 grv (layer-toggle layers))
  ;; tap: escape, hold: left control
  cec (tap-hold-press 200 200 esc lctl)
  gal (layer-switch gallium)
  qwr (layer-switch qwerty)

)

(deflayer layers
  _       f1     f2   f3   f4   f5   f6   f7   f8   f9   f10  f11  f12
  _    @qwr @gal _    _    _    _    _    _    _    _    _    _    _
  _    _    _    _    _    _    _    _    _    _    _    _    _    _
  _    _    _    _    _    _    _    _    _    _    _    _    _
  _    _    _    _    _    _    _    _    _    _    _    _
  _    _    _              _              _    _    _
)
